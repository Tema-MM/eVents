<h1>Thank You for Your Purchase!</h1>
<p>Here are your tickets:</p>
<ul>
  <% @purchases.each do |purchase| %>
    <li><%= purchase.event.name %> - <%= purchase.quantity %> ticket<%= purchase.quantity > 1 ? 's' : '' %> for <%= number_to_currency(purchase.total_price) %> (Purchased on <%= purchase.purchased_at.strftime("%d %b %Y %H:%M") %>)</li>
  <% end %>
</ul>
<p>Enjoy your event!</p>